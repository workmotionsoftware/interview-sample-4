# frontend

Two components from the admin app that lists orders. They are here to be **read and discussed**, not
built — there is deliberately no `package.json`, no bundler and no test runner, so nothing to install.

`OrderList.jsx` fetches orders for the current organization and renders a filterable list.
